# OpenClaw-Lingua: Standalone Migration — Work in Progress

## Goal

Move from an OpenClaw-dependent app to a standalone Python daemon that:
1. Calls local llama.cpp models for translation + tutoring
2. Uses local TTS (already done — OmniVoice via OpenAI-compatible API)
3. Manages Telegram message delivery + interactive LLM chat
4. Schedules lessons locally (replacing OpenClaw cron)

## Assessment (~65% new code, ~35% reuse)

### ✅ Already standalone — keep as-is
- `wikipedia_fetcher.py` — Kiwix/ZIM client
- `news_fetcher.py` — RSS feed fetching
- `fetch_router.py` — content dispatch
- `tts.py` — OmniVoice TTS (already uses OpenAI-compatible API)
- Config/profile system, vocab tracking

### ✅ DONE: Modified
- `orchestrator.py` — Replaced PAYLOAD markers with Orchestrator class, async run_lesson() pipeline calling llama_client + telegram delivery
- `processor.py` — Stripped instruction generation; now handles vocabulary markdown persistence only

### ✅ DONE: `src/llama_client.py` (20 tests)
Three operations over llama.cpp's OpenAI-compatible `/v1/chat/completions`:
translate(), extract_vocab(), tutor_chat()

### ✅ DONE: `src/telegram_bot.py` (29 tests)
aiogram 3.x Telegram bot — lesson delivery + interactive tutor chat with SQLite history

### ✅ DONE: `src/scheduler.py` (20 tests)
APScheduler daily lessons — delegates to Orchestrator.run_lesson()

### ✅ DONE: `src/main.py` (20 tests)
Daemon entry point — wires bot + scheduler concurrently, signal handling, logging

### ✅ DONE: `src/orchestrator.py` (18 tests)
Orchestrator class with async `run_lesson()` pipeline:
  fetch → clean → TTS → translate (llama_client) → vocab extraction → save (processor) → deliver
Keeps utility functions: `load_config()`, `get_profile()`, `clean_content()`, `fetch_article()`
CLI entry point for standalone runs.

### ✅ DONE: `src/processor.py` (15 tests)
Stripped OpenClaw-era instruction generation. Now handles vocabulary persistence only:
  `_ensure_vocab_file()`, `_read_existing_vocab()`, `update_vocab()`
Fixed markdown header parsing bug (`_read_existing_vocab`).

### ✅ DONE: `src/scheduler.py` (20 tests)
Removed duplicate lesson pipeline — now delegates to `Orchestrator.run_lesson()`.
Keeps schedule discovery, APScheduler integration, CLI listing.

### ✅ DONE: Config updates (`config.json`)
- Added `llm.base_url` → `http://llama-swap:8080/v1`
- Added `llm.default_model` → `gemma-4-26B-language` (tuned language model)
- Added `llm.api_key` placeholder
- Added `telegram.bot_token` placeholder
- Added per-user `telegram_chat_id` to profiles

## Test status
- `test_llama_client.py` — 20/20 ✅
- `test_telegram_bot.py` — 29/29 ✅
- `test_scheduler.py` — 20/20 ✅
- `test_main.py` — 20/20 ✅
- `test_orchestrator.py` — 18/18 ✅
- `test_processor.py` — 15/15 ✅
- **Total: 122 tests passing** (all mocked, zero real LLM calls)

Pre-existing failures (missing deps: requests, feedparser, kiwix):
- `test_news_fetcher.py` — 35 failed, 7 errors
- `test_wikipedia_fetcher.py` — 19 failed
- `test_fetch_router.py` — 1 failed

## Conda environment
- `lingua` (Python 3.11) — aiogram, openai, pytest, pytest-asyncio, apscheduler

## Status: ✅ COMPLETE
All migration tasks done. Config updated with:
- `llm` section pointing to `gemma-4-26B-language` on `llama-swap:8080`
- `telegram.bot_token` (fill in your bot token)
- `telegram_chat_id` per profile (fill in your chat ID)

Available models on llama-swap:
- `Qwen3.6-27B`
- `gemma-4-26B` (base)
- `gemma-4-26B-language` (tuned for language tasks ← current default)
- `gemma-4-E4B` (edge/small)
- `omnivoice` (TTS)

Per-profile model overrides supported via:
- `llm_model` — generic override for all tasks
- `llm_translate_model` — translation-specific
- `llm_tutor_model` — tutoring-specific
